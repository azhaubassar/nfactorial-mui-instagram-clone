import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FF0000",
      light: "#00FF00",
    },
    secondary: {
      main: "#FF0000",
      light: "#000",
    },
    buttonBg: {
      main: "#000",
      light: "#000",
    },
  },
});
